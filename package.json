{
  "name": "verse",
  "private": true,
  "workspaces": ["api-server", "client", "worker"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "packageManager": "bun@1.3.8",
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
